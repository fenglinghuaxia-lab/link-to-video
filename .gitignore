# 登录态 —— 含知乎 cookie，绝对不能提交到公开仓库
assets/storage_state.json

# 曲库清单缓存（939KB，可用 fetch_bgm.py 重建）
assets/bgm/_pieces_cache.json

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/

# 运行产物与临时文件
_raw/
*.log
.DS_Store
Thumbs.db

# 工作目录（处理文章时的中间产物，不进仓库）
capture_*/
screenshots/
video_frames/
*.mp4
